#include<stdio.h>
void main()
{
    int year;
    printf("Enter a year")
    scanf("%d",&year);
    {
        printf("%d is a leap year",year);
    }
    else
    { 
        printf("%d is a leap year",year);
    }
    else
    {
        printf("%d is not a leap year",year);
    }
    else
    {
        printf("%d is not a leap");
    }
}